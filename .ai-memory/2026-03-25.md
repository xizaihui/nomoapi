# 2026-03-25 Daily Log

## OpenToken Deployments
- **Commit `97f657c7`**: 靛蓝色彩体系 + 状态色统一 + Dropdown崩溃修复 — deployed to all 3 environments
- **Commit `3a99dc95`**: Form.Checkbox children文字显示 + onChange透传保存 — deployed to all 3 environments
  - Bug 1: 系统设置→配置登录注册 勾选框没有文字标签（FormCheckbox 只接收 `label` prop，未处理 `children`）
  - Bug 2: 勾选后不保存到后端（`onChange` 未透传，`handleCheckboxChange` → `updateOptions()` 从未被调用）
  - Fix: `displayLabel = label || children`; 显式解构 `onChange` 并在 input 事件中调用

## Production Server Security Audit (38.58.59.161)
- **结论: 未被黑**，进程/端口/目录均正常
- **每日重启原因**: VPS 供应商的 `/tmp/network_script` 在每次启动时执行网络初始化，SSH host key 被重新生成（导致 SSH fingerprint 变化）
- 重启不是 crontab/unattended-upgrades 触发的，是供应商侧行为
- **SSH 暴力破解**: 19,253 次失败尝试 — PermitRootLogin=yes, PasswordAuthentication=yes, PubkeyAuthentication=no
- **建议**: 开启 PubkeyAuthentication、安装 fail2ban、联系供应商确认重启策略
- SSH host key 每次重启都会变 → 连接时需要 `StrictHostKeyChecking=no` 或清理 known_hosts

## Pairing Attempt
- 未知号码 `+8613661356411` 尝试通过 WhatsApp 配对 OpenClaw，配对码 `5646DDAP`
- 已提醒用户确认是否认识，建议不认识则忽略
