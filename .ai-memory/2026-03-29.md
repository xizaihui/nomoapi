# 2026-03-29 工作日志

## 审计日志修复部署到测试+生产

### 背景
- commit `b03de076` 之前只部署了 dev 环境
- 测试 (154.36.173.198) 和生产 (38.58.59.161) 还停留在 `f74560bf`
- 生产审计率仅 ~6% (PG 8287 → ES 489)

### 部署操作
1. Git push feat/shadcn-ui + main → nomoapi + opentoken 双 remote
2. `deploy.sh test prod` 一键部署
3. 三环境确认 `b03de076` 已部署，HTTP 200 健康

### 修复内容回顾 (b03de076)
- `ExtractPromptFromRequest`: 从后往前遍历 user 消息，跳过纯 tool_result
- 新增 `extractClaudeToolResultContent`: fallback 从 tool_result content 提取摘要
- 新增 `extractResponsesInput`: OpenAI Responses 格式支持
- prompt 为空 → 标记 `[prompt_extraction_empty]` 照常写入
- `gopool.Go` 加 recover
- 移除 `TextHelper` 重复调用
- gin.Context 数据在主 goroutine 收集

### 测试文件
- 新增 `audit/middleware_test.go`: 8 个测试覆盖 Claude string/array/tool_result/mixed 格式
- 7/8 PASS，1 个 OpenAI 格式测试因 `Content: any` vs `json.RawMessage` 序列化差异需要调整

### 待观察
- 部署后生产审计率应恢复到接近 100%
- 过几小时可查 ES `audit-logs-2026.03` 数据量确认
