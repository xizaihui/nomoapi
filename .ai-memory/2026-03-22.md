# 2026-03-22 Session Notes

## Summary
今日完成了大量 UI 精修和基础设施改进，共 9 个 commit（04963cd9 → 9c5e03fe）。

---

## 审计侧栏图标差异化 — commit `18438b65`
- 审计日志 → `FileSearch`, 审计规则 → `ShieldCheck`, 保存策略 → `Timer`
- 添加到 `getLucideIcon()` switch in `helpers/render.jsx`

## 色彩体系重构 (Steel Blue/钢蓝灰) — commit `19cbc627`
- **核心**: 全站色彩从紫灰 (hue 240) → 纯中性灰 + 钢蓝点缀 (hue 215)
- **设计理念**: 高端、现代、稳重 — 低饱和度、高质感
- 涉及文件: `index.css`, `useDashboardCharts.jsx`, `useDashboardStats.jsx`, `render.jsx`, `StatsCards.jsx`, `Avatar.jsx`, `PreferencesSettings.jsx`, `ModelPricingEditor.jsx`
- Light primary: `hsl(215 20% 30%)` ≈ `#3d4f60`
- Dark primary: `hsl(215 18% 68%)` ≈ `#9db0c0`
- Background: 纯中性灰 `0 0%` (零色相)

## 侧栏菜单名称精简 (两轮)
### Round 1 — commit `19cbc627`
- 概况预览→概览, 令牌管理→令牌, 日志详情→日志, 渠道管理→渠道, 用户管理→用户

### Round 2 — commit `fa8a08ea`
- 模型管理→模型, 兑换码管理→兑换码, 审计日志→日志, 审计规则→规则
- 7 语言 i18n 全部同步

## 保存策略合并到规则页面 — commit `fa8a08ea`
- AuditRulesPage.jsx 重构为 Tab 页面:「审计规则」|「保存策略」
- 侧栏移除独立"保存策略"入口
- `/console/audit-retention` → Navigate redirect 到 `/console/audit-rules`

## Git main 分支合并 — commit `5d8aac01`
- `feat/shadcn-ui` fast-forward 合并到 `main`（122 commits）
- 推到 `nomoapi` + `opentoken` 两个 remote

## 安全部署脚本 — commit `5ac15797`
- `scripts/deploy.sh` — 一键三环境部署
- **核心改进**: Docker build 失败不执行 compose up，旧容器继续运行
- 远程先 git pull 再 build（修复 dist 被旧代码覆盖问题）
- 用法: `./scripts/deploy.sh [local|test|prod]`

## Form.Upload 重写 — commit `9c5e03fe`
- **问题**: Vertex AI 渠道上传 JSON 密钥文件，双击选文件后页面崩溃
- **根因**: FormField 的 cloneElement 注入 value 到 file input，浏览器安全限制禁止
- **修复**: 完整重写 FormUpload（146行），支持拖拽/多文件/受控 fileList/onChange 回调兼容 Semi

## 生产环境事件
- **部署不生效**: 旧流程只 SCP dist 不 git pull，Go embed 用旧代码覆盖新 dist → 已修复为先 pull 再 build
- **服务器异常重启** (15:38 UTC): 疑似机房维护，ES 启动吃 120% CPU 导致 ~2 分钟不可用，后自动恢复
- **教训**: 远程部署必须先 git pull；deploy.sh 已固化此流程

## 最终状态
- HEAD: `9c5e03fe` on `feat/shadcn-ui` = `main`
- 三环境全部部署 `9c5e03fe`
- Commit 链: `04963cd9 → 18438b65 → 19cbc627 → 904fc9ff → 5d8aac01 → 65e061c4 → fa8a08ea → 561a9ce5 → 5ac15797 → 9c5e03fe`
