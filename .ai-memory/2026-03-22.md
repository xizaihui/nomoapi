# 2026-03-22 Session Notes

## Sidebar Icon Differentiation — commit `18438b65`
- Changed 3 audit menu items from sharing same default icon to unique Lucide icons:
  - 审计日志 (audit-logs) → `FileSearch`
  - 审计规则 (audit-rules) → `ShieldCheck`
  - 保存策略 (audit-retention) → `Timer`
- Added icons to `getLucideIcon()` switch in `helpers/render.jsx`
- Deployed to all 3 environments at `18438b65`

## Color Scheme Overhaul — Steel Blue (钢蓝) Theme
- User requested: 高端、现代、稳重风格 — 黑白灰基调 + 低饱和点缀色
- **Major change**: Migrated entire color system from purple-tinted gray (hue 240) → neutral gray + steel blue accent (hue 215)
- Files changed:
  - `index.css` — all CSS variables (light + dark), blur balls, pastel balls, shine text
  - `useDashboardCharts.jsx` — VChart light/dark color schemes (10-color palettes)
  - `useDashboardStats.jsx` — stat card trend colors + avatar colors neutralized
  - `render.jsx` — base 10-color, extended 20-color, model color map
  - `StatsCards.jsx` — sparkline colors
  - `Avatar.jsx` (compat) — all color mappings → foreground opacity variants
  - `PreferencesSettings.jsx` — violet → neutral
  - `ModelPricingEditor.jsx` — tag colors → grey/white
- Light theme primary: `215 20% 30%` (steel blue dark gray)
- Dark theme primary: `215 18% 68%` (steel blue light)
- Background: pure neutral `0 0%` (no hue tint)
- Deployed to local dev only — user reviewing before production push
- **Docker rebuild gotcha**: First deploy served old CSS because Docker built before `bun run build` completed for color changes. Had to rebuild Docker image again.

## Key Color Values (new scheme)
- Light primary: `hsl(215 20% 30%)` = ~#3d4f60
- Dark primary: `hsl(215 18% 68%)` = ~#9db0c0
- Chart base: `#2e3a48, #4a5c6e, #6b7f8f, #8fa0ae, #b3c2cc`
- Blur balls: `#4a5c6e` (dark steel) + `#8fa0ae` (light steel)

## Current State (updated 14:44 UTC)
- All 3 environments deployed at `19cbc627` (color overhaul + sidebar simplification)
- `feat/shadcn-ui` merged into `main` via fast-forward → both at `5d8aac01`
- Pushed `main` to both `nomoapi` and `opentoken` remotes
- Current code HEAD: `65e061c4` (sync script commit on feat/shadcn-ui)

## Sidebar Menu Renaming (round 2) — pending commit
- 模型管理 → 模型, 兑换码管理 → 兑换码, 审计日志 → 日志, 审计规则 → 规则
- i18n keys added for all 7 languages: 模型/兑换码/规则
- Files: SiderBar.jsx, SettingsSidebarModulesAdmin.jsx, NotificationSettings.jsx, RedemptionsDescription.jsx, AuditRulesPage.jsx

## 保存策略 merged into 规则页面 — pending commit
- AuditRulesPage.jsx now has Tab UI: 「审计规则」|「保存策略」
- Retention panel code moved inline (imports from same API module)
- Sidebar "保存策略" entry removed from SiderBar.jsx
- App.jsx: AuditRetentionPage lazy import removed, `/console/audit-retention` → Navigate redirect to `/console/audit-rules`
- AuditRetentionPage.jsx file still exists but is no longer referenced
- Local dev deployed and verified (200)
- Waiting for user approval before pushing to git + deploying to test/prod

## Git Branch Status
- `main` = `5d8aac01`, pushed to both remotes
- `feat/shadcn-ui` = `65e061c4` (latest committed), uncommitted changes for sidebar rename + retention merge
- Stash popped, working tree has uncommitted changes
