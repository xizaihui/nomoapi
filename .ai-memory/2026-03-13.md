# 2026-03-13

## New API 项目 Semi → shadcn/ui 迁移

用户：抱抱熊 (WhatsApp +2348134196480)
项目路径：`/opt/apps/newapis/web/`
源码仓库：`https://github.com/QuantumNous/new-api.git`
Git origin：`https://xizaihui:github_pat_***REDACTED***@github.com/xizaihui/opentoken.git`

### 构建注意事项
- `NODE_OPTIONS="--max-old-space-size=4096"` 必须设置
- `--legacy-peer-deps` 安装依赖时必须加
- Docker 容器 `new-api` 运行在端口 3000

### 迁移进度（6步计划）
- [x] Step 1: 基础设施搭建 — Radix UI、cva、tailwind-merge、tailwindcss-animate 已安装；tailwind.config.js 重写为 shadcn CSS 变量色彩系统；index.css 和 theme-override.css 已加入 shadcn 主题变量；cn() 工具函数已创建
- [x] Step 2: shadcn/ui 基础组件库 — 22 个组件在 `src/components/ui/`：button, card, dialog, input, textarea, label, tabs, dropdown-menu, select, tooltip, popover, checkbox, radio-group, switch, avatar, progress, separator, scroll-area, badge, skeleton, accordion, table, collapsible, sheet, spinner + index.js 统一导出
- [x] Step 3: Semi → shadcn 适配层 — 21 个适配组件在 `src/components/compat/`：Button, Modal, Typography, Card, Tag, Spin, Empty, Form, Input, Dropdown, Tooltip, Popover, Banner, Avatar, Space, Skeleton, Checkbox, Switch, Radio, Tabs, Row/Col, Divider, Select, Progress, Descriptions, Toast, Badge + index.js 统一导出
- [x] Step 4: 替换布局层 — PageLayout(div+Tailwind), SiderBar(纯Tailwind+cn), Footer(span+Tailwind), HeaderBar(已是shadcn-ready)；semi-ui chunk 1,801→1,772 kB
- [x] Step 5: 逐模块页面替换 — 3轮批量脚本替换完成，260+75+13=348个文件迁移；新增15个compat组件(Table,SideSheet,Popconfirm,InputNumber,Collapse,TextArea,Steps,Pagination,ImagePreview,DatePicker,Timeline,Collapsible,Layout,Icon,Slider)；92个文件semi-icons→lucide-react映射(compat/icons.jsx)
- [~] Step 6: 清理进行中 — 新增compat组件(Notification,Highlight,SplitButtonGroup,AvatarGroup,List,TagInput,InputGroup,ScrollList/ScrollItem,Image,Calendar,LocaleProvider)；icons.jsx补全所有缺失图标映射；仅剩1个文件(ChatArea.jsx)直接引用semi-ui的Chat组件

### 项目统计
- 379 JS/JSX 文件总计
- 272 文件 import `@douyinfe/semi-ui`
- 92 文件 import `@douyinfe/semi-icons`
- Top Semi 组件使用量：Button(77), Modal(50), Typography(47), Card(31), Form(29), Spin(28), Tag(22), Empty(19)

### 关键决策
- 适配层方式：compat 组件保持 Semi API，内部用 shadcn 实现，只需改 import 路径
- 保留 Tailwind CSS v3，不升级
- Semi 和 shadcn 共存过渡期
- Toast 适配层暂用 react-toastify（项目已有依赖）
- 低频组件（Layout, Nav, Table, Pagination, SideSheet, Steps 等）暂保留 Semi 原生

### 构建验证
- Step 1 构建通过 (2m 19s)
- Step 2 构建通过 (2m 23s)  
- Step 3 构建通过 (2m 29s)
- Step 4 构建通过 (2m 25s)
- Step 5 构建通过 — semi-ui chunk 1,801→1,304 kB (-28%)
- Step 6 构建通过 (2m 23s) — semi-ui chunk 1,304→1,119 kB (-38% total)

### Step 6 当前状态 (15:50 UTC)
- semi-ui chunk: 1,801 kB → 1,119 kB (减少38%)
- `@douyinfe/semi-icons` 引用: 92→0 个文件（全部替换为 compat/icons.jsx lucide映射）
- `@douyinfe/semi-ui` 引用: 272→1 个文件（仅 ChatArea.jsx 的 Chat 组件）
- 迁移脚本位置: `scripts/migrate-semi-imports.cjs`, `migrate-semi-imports-r2.cjs`, `migrate-semi-imports-r3.cjs`, `migrate-semi-icons.cjs`
- compat 层总计 ~45 个适配组件 + icons.jsx 图标映射
- 待决策：是否替换 Chat 组件以完全移除 semi-ui 依赖
