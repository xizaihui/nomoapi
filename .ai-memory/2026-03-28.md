# 2026-03-28 工作日志

## 模型鉴真自动巡检引擎 — 完成并部署

### 后端实现
- `common/constants.go`: 新增 `ChannelStatusVerifyDisabled = 4`（鉴真禁用状态）
- `model/channel.go`: 新增字段 `auto_verify`(*int), `verify_model`(*string), `last_verify_time`(int64), `last_verify_score`(int) + `GetAutoVerifyChannels()`, `UpdateChannelVerifyResult()` 方法
- `controller/model_verify_patrol.go`: 巡检引擎（154行）
  - 每1分钟扫描，每5分钟对 auto_verify=1 的渠道执行鉴真
  - 启用中 + 分数<70 + 连续2次 → status=4 禁用 + NotifyRootUser
  - 鉴真禁用 + 分数≥70 → status=1 恢复 + NotifyRootUser
  - 网络错误不触发禁用逻辑
- `controller/channel-test.go`: 系统自带 testAllChannels 跳过 status=4
- `main.go`: 启动 `go controller.StartAutoVerifyPatrol()`

### 前端实现
- `EditChannelModal.jsx`: 新增「自动鉴真」开关 (auto_verify 字段)
- `ChannelsColumnDefs.jsx` + `ChannelSelectorModal.jsx`: status=4 显示橙色「鉴真禁用」标签

## SplitButtonGroup 按钮高度对齐修复
- 问题: 测试按钮(size=small → h-8)旁边下拉箭头(icon-only → h-9)高度不一致
- `web/src/components/ui/button.jsx`: 新增 `icon-sm` 变体 (h-8 w-8)
- `web/src/components/compat/Button.jsx`: icon-only + size=small → 使用 icon-sm

## Git & 部署
- Commits: `c3f513fd`(巡检引擎), `277b8979`(按钮对齐), `94eb8377`(newapi.md docs)
- 三环境同步部署完成 @ `94eb8377`: dev ✅, test ✅, prod ✅
- feat/shadcn-ui + main 推送到 nomoapi + opentoken 双 remote
- newapi.md 已更新完整记录

## 上游同步 — 🔴 关键修复 cherry-pick (下午)

### Commit `5ff73e35`: 5个关键修复合并
1. **`ded4a124`** — `dto/openai_request.go`: `MessageImageUrl.Detail` JSON tag 加 `omitempty`，防止空 detail 字段发到上游
2. **`62b9aaa5`** — `relay/channel/task/taskcommon/helpers.go`: `UnmarshalMetadata()` 删除 `metadata["model"]` 防止计费模型被覆盖
3. **`e520977e`** — `relay/channel/claude/adaptor.go` + `relay/common/relay_info.go`: Claude beta query 时序修复，新增 `shouldAppendClaudeBetaQuery()` 在最终 URL 阶段追加而非提前
4. **`b09337e6`** — `middleware/distributor.go`: channel affinity 首选渠道被禁用时检查 skip-retry 标志，禁用则直接 403；`service/channel_affinity.go`: `ShouldSkipRetryAfterChannelAffinityFailure()` 先查 context 再查 meta.SkipRetry
5. **`926e1781`** — `service/convert.go`: 新增 `buildClaudeUsageFromOpenAIUsage()` 保留 cache usage (CachedCreationTokens, CachedTokens, 5m/1h) 在 OpenAI→Claude 响应转换中

### Commit `5ead0d0b`: 计费重构大合并
- **`9ecad906`** — 计费流程重构: `PostClaudeConsumeQuota` (service/quota.go) + `postConsumeQuota` (relay/compatible_handler.go) → 统一为 `PostTextConsumeQuota` (service/text_quota.go 新文件 427行)
  - 所有 handler (compatible/claude/gemini/embedding/rerank/image/audio/responses) 调用点迁移
  - relay/compatible_handler.go 从 512→227 行
  - service/quota.go 移除 PostClaudeConsumeQuota (101行)
- **`d4a470a6`** — OpenRouter 计费语义修复: 移除 `isOpenRouter` 独立变量，改用 `isOpenRouterClaudeBilling` (ChannelMeta+ChannelTypeOpenRouter+IsClaudeUsageSemantic)；cache token 从 prompt 分离计费正确
- 新增字段: `dto.Usage.UsageSemantic`, `dto.Usage.UsageSource`, `relaycommon.RelayInfo.ParamOverrideAudit`
- 新增函数: `appendFinalRequestFormat()`, `appendParamOverrideInfo()` in log_info_generate.go
- text_quota_test.go: 7个测试全部 PASS

### ⚠️ 安全修复 — 审计日志权限隔离

**漏洞**: 普通用户获得审计权限后，可以看到同 group 所有用户的审计日志（含完整 prompt 内容）
**严重性**: 高 — 用户数据泄露

**Commit `af63cb08`**: security: 修复审计日志权限隔离
- `audit/elasticsearch.go`: SearchParams 新增 `UserId` 字段，SearchAuditLogs 查询增加 user_id term 过滤，GetAuditStats 签名改为 `(group string, userId int, startTime, endTime int64)`
- `audit/controller.go`:
  - `SearchAuditLogsHandler`: 非管理员 → 强制 `params.UserId = userId`，清空 username/group 参数防手工构造
  - `GetAuditStatsHandler`: 非管理员 → 强制按 userId 统计
  - 管理员不受影响，仍可查看所有
- 本地 dev 已部署 ✅，待推送到测试/生产

### 当前状态
- 本地 dev @ `af63cb08` ✅
- 测试/生产待部署（用户确认后推送）
- 前端无变更（纯后端修复）
