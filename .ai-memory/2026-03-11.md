# 2026-03-11

## 02:00-02:41 UTC — v1.1.0 Deployed: OpenToken Rebrand + Charts + ES

### What happened
- Completed OpenToken rebrand (New API → OpenToken): system name, logo (SVG/PNG/favicon), HTML title, about page, startup log
- Added VChart visualization to audit pages (admin + company leader views):
  - Bar chart: daily request trend (7/15/30 day selector) with total/violations/suspicious
  - Donut chart: risk level distribution with center indicator
- Built full Elasticsearch integration layer (`service/audit_es.go`):
  - Dual-write: PG (primary) + ES (search/aggregation), ES optional via `ES_URL` env var
  - Bulk indexing: 500 docs/batch, 3s flush, daily index rotation `opentoken-audit-YYYY.MM.DD`
  - Full-text search, aggregations (daily trend, risk distribution, model usage, top employees, company breakdown)
  - Auto-cleanup expired ES indices synced with retention policy
  - Graceful degradation: ES unavailable → auto fallback to PostgreSQL
- Added `/api/audit/charts` (admin) and `/api/user/audit/charts` (company leader) endpoints
- Added `model.GetAuditDailyTrend()` for PG-based daily trend aggregation
- docker-compose.yml updated with ES service (commented, ready to enable)
- Cleaned up 609 old upstream tags locally, only v1.0.0 + v1.1.0 remain
- Committed and pushed to GitHub as v1.1.0

### Key files added/modified
- `service/audit_es.go` — NEW, full ES integration (init, batch write, search, aggregations, cleanup)
- `middleware/audit.go` — added `EnqueueAuditLogToES()` dual-write calls
- `service/audit_cleaner.go` — added ES index cleanup hook
- `controller/audit.go` — added `GetAuditChartData()`, `GetMyAuditCharts()`
- `model/audit_log.go` — added `DailyTrendItem`, `GetAuditDailyTrend()`
- `router/api-router.go` — added `/audit/charts` and `/user/audit/charts` routes
- `web/src/pages/Audit/index.jsx` — added VChart imports, chart state, trend bar chart + risk donut
- `web/src/pages/Audit/MyAuditLog.jsx` — added VChart trend + risk charts for company leaders
- `docker-compose.yml` — ES service config (commented), ES_URL env var

### Charts API tested
- `GET /api/audit/charts?days=7` returns `source: postgresql` with daily_trend + stats
- When ES_URL is set, returns `source: elasticsearch` with full aggregations

## 02:35 — UI Redesign Request: OpenRouter Style

### User request
- 抱抱熊 wants to completely restyle OpenToken UI to match OpenRouter.ai's design
- OpenRouter style: deep dark theme (#0a0a0a), minimalist, green accent color, no glassmorphism, no gradients, compact data tables, thin/no borders
- This is the OPPOSITE of current Aurora theme (violet/rose gradients, glassmorphism, blur balls)
- Also mentioned: "logo 网页还没改过来" — some frontend logo references may still show old branding

### Work estimate
- ~4-5 hours total, pure CSS/style layer, no backend changes needed
- No need for phased development — single build cycle
- Awaiting user confirmation to start

### Current system state
- Server: 154.40.40.48:3000, Docker containers: new-api, postgres, redis
- GitHub: https://github.com/xizaihui/opentoken (v1.1.0)
- All 6 audit steps complete, performance optimized, retention policy, ES layer ready
