# 2026-03-14 Memory Log

## User: 抱抱熊 (+2348134196480, WhatsApp)
- First interaction, bootstrap session — identity/user files still blank
- Speaks Chinese, technical user, comfortable with DevOps and frontend

## New API Deployment
- Cloned https://github.com/QuantumNous/new-api.git to /opt/apps/newapis
- Built Docker image `newapi-aurora:latest` (needed 4GB swap to survive vite build OOM)
- Running via docker-compose: new-api + postgres + redis
- Access: http://154.40.40.48:3000
- Default admin: root / 123456

## shadcn/ui Migration Project (feat/shadcn-ui branch)
- Goal: Replace Semi Design UI with shadcn/ui while preserving all functionality and enabling easy upstream sync
- Strategy: Vite alias + compat layer (Semi API surface → shadcn/ui internals)

### Phase 0 — DONE
- Installed all shadcn/ui deps (radix-ui, cva, tailwind-merge, sonner, tanstack-table, etc.)
- Added shadcn CSS variables (light+dark) to index.css, coexisting with Semi vars
- Extended tailwind.config.js with shadcn color system + animations
- Created 21 shadcn/ui base components in src/components/ui/
- Created src/lib/utils.js (cn helper)
- Added components.json
- Vite alias: @douyinfe/semi-ui__real points to real Semi; commented alias ready to redirect @douyinfe/semi-ui → compat layer

### Phase 1 — DONE
- Built 31 compat wrappers in src/components/compat/:
  Avatar, Badge, Banner, Button, Card, Checkbox, Descriptions, Divider, Dropdown, Empty, Input/TextArea, InputNumber, Layout(Row/Col), Modal, Pagination, Popconfirm, Popover, Progress, Radio/RadioGroup, Select, SideSheet, Skeleton, Space, Spin, Steps, Switch, Tabs/TabPane, Tag, Toast/Notification, Tooltip, Typography(Text/Title/Paragraph)
- 12 components still pass-through from real Semi: Chat, Collapse, Form, Image, ImagePreview, Layout, LocaleProvider, Nav, Slider, Table, Timeline, Icon
- Barrel index.js exports everything; alias switch is one uncomment away

### Phase 2 — DONE (02:30-03:30 UTC)
- Activated Vite alias: bare `@douyinfe/semi-ui` → compat layer
- Used array-style alias config to handle deep path imports (`/dist`, `/lib`) separately
- Fixed Tag.jsx syntax (hyphenated keys need quotes)
- Discovered additional components needed during build: Collapsible, TagInput, InputGroup, Highlight, SplitButtonGroup, List, AvatarGroup, ScrollList/ScrollItem, DatePicker, Calendar
- Created all missing compat wrappers
- Semi icons compat layer written (lucide-react mappings in icons.js) but NOT activated — Semi internal components also import from @douyinfe/semi-icons, would break pass-through components
- Installed deps via bun (tailwindcss-animate was missing from bun install)
- Build: 18277+ modules, passes in ~2m29s
- `antd` missing error from @lobehub/icons is pre-existing (not our issue)

### Phase 2.5 — DONE (03:30 UTC)
- Migrated 9 more complex components out of real Semi pass-through:
  Layout(Header/Content/Footer/Sider), Nav, Icon, Collapse(Panel), Slider, Timeline(Item), Image/ImagePreview, LocaleProvider

### Phase 3 — DONE (04:00 UTC)
- Migrated Table with full compat: sorting, pagination, row selection, scroll, expandedRowRender, fixed columns, filtering
- Only 2 components still pass through from real Semi: **Chat** and **Form**
- Form is the hardest — 29 file references, formApi state management, 13 sub-components (Form.Input/Select/Switch/TextArea/InputNumber/Checkbox/DatePicker/AutoComplete/Upload/Section/Slot/RadioGroup/TagInput)
- Chat used in 1 place (Playground ChatArea) — complex chat UI with ref, roleConfig, streaming
- Asked user whether to keep Form/Chat on real Semi or migrate them too — awaiting response

### Build Notes
- Must use `NODE_OPTIONS="--max-old-space-size=4096"` for bun/vite build (OOM otherwise)
- bun installed at `~/.bun/bin/bun`
- Build command: `export PATH="$HOME/.bun/bin:$PATH" && cd /opt/apps/newapis/web && NODE_OPTIONS="--max-old-space-size=4096" bun run build`

### Git Commits on feat/shadcn-ui
- eef12e7c: Phase 1 compat layer (31 components)
- cdd42836: Phase 2 activate alias + fix missing exports
- fa117dcf: Phase 2.5 migrate 9 complex components
- 8d028e0a: Phase 3 migrate Table

## Server Notes
- Host: cn2965035643421, IP 154.40.48, Linux 6.8.0-48-generic
- 7.8GB RAM, added 4GB swap at /swapfile for builds
- Docker 29.3.0, Compose v5.1.0
- No Go installed on host (builds happen in Docker)

### Deployment (04:17 UTC)
- Rebuilt Docker image with `--no-cache`: 18296 modules compiled, build passed
- `docker compose down && docker compose up -d` — all 3 containers running
- Service confirmed live at http://154.40.40.48:3000 (API status OK)
- New image sha: f2d7cddb24d2 (then rebuilt no-cache)
- User asked to keep Chat component on real Semi (Playground page to be hidden from users later)
- User reviewing live site for visual issues

### Decision: Form & Chat
- Chat stays on real Semi — Playground page will be hidden from end users
- Form migrated after all (see Phase 4 below)

### Phase 4 — DONE (04:35 UTC)
- Migrated Form to full compat wrapper with custom formApi implementation
  - formApi: getValues, getValue, setValue, setValues, reset, validate
  - 13 sub-components: Form.Input, Form.TextArea, Form.InputNumber, Form.Select, Form.Switch, Form.Checkbox, Form.RadioGroup, Form.DatePicker, Form.AutoComplete, Form.Upload, Form.TagInput, Form.Section, Form.Slot
  - FormContext provides formApi to all sub-components
  - Supports getFormApi prop, ref forwarding, initValues, onValueChange, layout (vertical/horizontal)
- Build passes: 2m 42s
- Deployed to Docker, service live
- Commit: d7ac1a49
- **Only Chat remains on real Semi** (1 reference in Playground ChatArea — page to be hidden)
- User confirmed pages look fine after Phase 3 deployment, asked to continue
- User reviewing Form migration deployment now (settings pages are key test area)

### Phase 5 — DONE (05:00 UTC) — Global Color System
- Built Semi → shadcn CSS variable bridge in index.css (:root and .dark)
- All `--semi-color-*` variables now derive from shadcn HSL variables
- 219 JSX/JS files referencing `--semi-color-*` work without modification via bridge
- Updated body font from Lato to Inter + system font stack
- Sidebar styles migrated to `hsl(var(--sidebar-*))` variables
- Border-radius unified to `var(--radius)` (0.625rem)
- Commit: 58f60f1b

### Phase 6 — DONE (05:03 UTC) — Layout Overhaul
- PageLayout: replaced Semi Layout/Header/Sider/Content with semantic HTML (div/header/aside/main/footer)
- HeaderBar: glassmorphism header with `bg-background/80 backdrop-blur-lg border-b border-border`
- Navigation: `text-muted-foreground` with `hover:text-foreground`
- SiderBar: replaced Semi Nav with custom Link-based nav items, collapsible sub-menus
- Sidebar highlight color uses `hsl(var(--sidebar-primary))`
- Commit: abc5ad3c

### Phase 7 — DONE (05:40 UTC) — Tailwind Class Migration
- Replaced all 75 `semi-color-*` Tailwind class references in JSX with shadcn tokens
- text-semi-color-text-0 → text-foreground, text-1 → text-foreground/80, text-2 → text-muted-foreground
- bg-semi-color-bg-0 → bg-background, fill-0 → bg-muted, etc.
- border-semi-color-border → border-border
- Dropdown overlays → bg-popover
- Commit: b321429d

### CRITICAL BUG FOUND (05:52 UTC)
- docker-compose.yml was still using `image: calciumion/new-api:latest` (upstream original)
- All deployments were running the ORIGINAL image, not our `newapi-aurora:latest`
- Fixed: changed image to `newapi-aurora:latest` in docker-compose.yml
- Redeployed — now running correct custom image
- User checking if changes are visible now

### Git Log (feat/shadcn-ui)
- eef12e7c: Phase 1 compat layer
- cdd42836: Phase 2 activate alias
- fa117dcf: Phase 2.5 complex components
- 8d028e0a: Phase 3 Table
- d7ac1a49: Phase 4 Form
- 58f60f1b: Phase 5 global colors
- abc5ad3c: Phase 6 layout overhaul
- b321429d: Phase 7 Tailwind class migration
- ab05b29b: Form.Input [object Object] fix (FormField _noInject pattern)
- 012673d9: Usage logs page fix (Table expandRowByClick, Form.Select.Option)
- 0d3450e0: Items 1-3 fix (Modal imperative API, DatePicker range, sidebar CSS)
- ccca3aa7: Items 4-5 fix (VChart theme, 250 inline semi-color vars replaced)

### Form.Input [object Object] Bug Fix (06:30 UTC)
- Root cause: FormField's `React.cloneElement(children, { value, onChange })` injected `value={object}` onto wrapper `<div>` elements
- Solution: `_noInject` prop on FormField — all self-managing Form sub-components (FormInput, FormTextArea, FormInputNumber, FormSelect, FormSwitch, FormCheckbox, FormRadioGroup, FormDatePicker) pass `_noInject` to skip cloneElement injection
- AutoComplete/Upload/TagInput/Section keep default FormField behavior (no `_noInject`)
- Also filtered `allowEmpty`, `validateFields`, `trigger` from Form's `...rest` spread to prevent DOM warnings

### Usage Logs Page Fix (07:21 UTC)
- Table: added `expandRowByClick` and `rowExpandable` props
- Form.Select: added `onChange` callback, `showClear`, `size` prop filtering
- Form.Select.Option: new sub-component for `<option>` rendering

### Items 1-3 Fix (08:04 UTC)
1. Modal.confirm/info/warning/error/success — full imperative API via createRoot portal
   - Returns `{ destroy, update }` handle (EditChannelModal uses `modal.destroy()`)
   - Supports custom footer, async onOk with loading, type-specific icons (lucide-react)
   - Supports `fullScreen`, `header`, `size` props on declarative Modal
2. Form.DatePicker — full `dateTimeRange` support
   - Dual datetime-local inputs, presets buttons, showClear
   - Single date/dateTime mode preserved with `_noInject`
3. Sidebar CSS — nav item default color, group label color use shadcn sidebar vars
4. Sonner `<Toaster>` mounted in PageLayout (was missing — Toast/Notification were no-ops)
5. Toast/Notification: added `id` support for `close(id)` pattern

### Items 4-5 Fix (08:36 UTC)
4. VChart theme: kept Semi theme bridge + added ThemeManager dark/light sync
5. Replaced ALL 250 `var(--semi-color-*)` inline style refs in 66 JSX/JS files:
   - border → hsl(var(--border)), fill-0 → hsl(var(--muted)), text-0 → hsl(var(--foreground))
   - text-2 → hsl(var(--muted-foreground)), primary → hsl(var(--primary))
   - bg-0/1/2 → background/card/muted, warning/success/danger → chart/destructive vars
   - 0 remaining semi-color refs in JSX/JS (excluding compat layer and index.css)

### Remaining Work (as of 08:56 UTC)
Items 6-11 (cleanup/slimming):
6. 96 `.semi-` CSS class selectors in index.css — depend on semi.css
7. 206 `semi-color-` in index.css — CSS bridge vars
8. semi.css still loaded in index.jsx — remove after 6+7
9. 88 files import @douyinfe/semi-icons — alias not activated yet
10. 35 @douyinfe/semi-illustrations imports
11. 63 unique Semi icons need lucide-react mapping (35 already mapped)

### Docker Image History
- newapi-aurora:latest rebuilt multiple times today
- Latest: sha256:1e63ff35f6c8 (items 4-5 fix, 08:36 UTC)

---

## Updates (10:00–14:00 UTC)

### Dropdown Blank Page Fix (commit d0ed833c)
- Dropdown.Item was nesting interactive elements (buttons inside DropdownMenuItem) → DOM anomalies → blank page
- Fixed: detect interactive children, wrap in styled `<div>` instead of `DropdownMenuItem`
- Dropdown.Menu → fragment wrapper, added Dropdown.Title sub-component

### Complete Icon Migration (commit d0ed833c)
- All 63 Semi icons mapped to lucide-react in `src/components/compat/icons.js`
- All 88 app-level files migrated from `@douyinfe/semi-icons` to `@/components/compat/icons`
- Item 9 and 11 from cleanup list: DONE
- `@douyinfe/semi-icons` Vite alias still NOT activated (Chat component uses real Semi internally)

### Select Empty Value Crash Fix (commit 0d246b52)
- Root cause of "测试" button blank page: Radix `<Select.Item>` rejects `value=""`
- ModelTestModal had `{ value: '', label: '自动检测' }` option
- Fix: EMPTY_SENTINEL (`__empty__`) in Select compat — maps empty string internally, converts back on onChange

### Table Compat Improvements (commit e6cdd7d1)
- Added `onSelectAll` callback support
- Added `currentPage` external sync via useEffect
- Added `onPageChange` callbacks on pagination buttons

### Modal/Banner Fixes
- Modal: added `size` prop support (SIZE_MAP: full-width/large/medium/small)
- Banner: `closeIcon={null}` now hides close button

### ErrorBoundary Added (in progress — building)
- `src/components/common/ErrorBoundary.jsx` — catches React render errors, shows fallback UI with retry button
- Wrapped `<Routes>` in App.jsx with `<ErrorBoundary>` — prevents full white screen on component crashes
- Build passed, Docker image building at ~14:00 UTC

### GitHub Push
- Created repo `xizaihui/nomoapi` on GitHub
- Pushed `feat/shadcn-ui` → `main` + tag `v1.0.0`
- Had to remove `.github/workflows/` (PAT lacks `workflow` scope)
- Also pushed to `xizaihui/opentoken` (pre-existing repo)
- GitHub user: `xizaihui`
- PAT stored in git remote URL (token: github_pat_***REDACTED***...)

### Upstream Sync Script
- Created `/opt/apps/newapis/sync-upstream.sh`
- Auto: fetch upstream, merge (auto-resolve compat/ui/layout conflicts), detect new Semi components/icons, fix semi-icons imports, build, docker deploy, optional GitHub push

### Full Project Audit (14:00 UTC)
Remaining cleanup items:
- 35 `@douyinfe/semi-illustrations` imports (empty state illustrations)
- 205 `semi-color-` refs in index.css (CSS bridge, functional)
- 96 `.semi-` class selectors in index.css (depend on semi.css)
- `semi.css` still loaded in index.jsx
- Calendar + DatePicker still pass-through (real Semi)
- 19 compat `<button>` elements missing aria-labels
- No ErrorBoundary previously (now added)
- 0 remaining semi-icons imports ✅
- 0 remaining inline var(--semi-color-) in JSX ✅
