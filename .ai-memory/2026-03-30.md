# 2026-03-30 工作日志

## 审计修复部署到测试+生产
- commit `b03de076` 之前只部署了 dev，今天推送到 test (154.36.173.198) 和 prod (38.58.59.161)
- Git push feat/shadcn-ui + main → nomoapi + opentoken 双 remote
- `deploy.sh test prod` 一键部署，三环境确认 HTTP 200

## 生产缓存数据分析
- 用户问 affinity 页面显示缓存命中率只有 17.86% (35/196) 是否太低
- 查了 PG 24h 全量数据：Opus 61.5% 请求命中率，64.4% token 缓存率（实际很好）
- Sonnet 30% 请求命中率，99.9% token 缓存率
- Affinity 页面只显示 TTL 滚动窗口内的数据，不代表全量
- 单 key + hash affinity 场景下缓存工作正常

## OpenClaw API 格式切换（未完成）
- 用户要求从 openai-completions 切到 anthropic-messages 享受 cache_control 缓存计费
- 尝试用 config.patch 修改，但发现 config 里 model 级别的 `api: openai-completions` 仍然覆盖了 provider 级别设置
- 需要再次修复：确保 model 级别也改为 anthropic-messages，baseUrl 去掉 /v1 后缀

## OpenClaw 记忆系统优化
- 启用 memorySearch：openai embedding（走 3mll 代理）
- 搜索源：memory 文件 + 历史会话 + newapi.md
- 混合搜索：向量 70% + 关键词 30%
- contextTokens: 128k → 200k
- compaction: keepRecentTokens 30k, maxHistoryShare 0.7
- memoryFlush: 启用（上下文快满时自动写回 memory）

## OpenToken 功能清单整理
- 整理了完整功能清单，区分上游继承 vs 新增 vs 大幅改造
- 纯新增 7 个模块：审计系统、审计保存策略、模型鉴真、鉴真巡检、多密钥哈希亲和、上游定价同步、Playground 开关
- 大幅改造 6 个方向：Semi→shadcn 迁移、Playground 重写、品牌差异化、部署体系、数据库调优
- 8+ 个自研 bug 修复

## LibreChat 集成（进行中）

### 部署
- LibreChat Docker: `/opt/apps/librechat/docker-compose.yml`
- MongoDB 7 + LibreChat latest, 端口 3080
- 共享账号: admin@opentoken.io / ***REDACTED***

### 集成方案
- Go 反向代理: `/chat/*` → LibreChat:3080（HTML base href 重写）
- 认证桥接: `POST /api/librechat/auth` → 自动登录 + 设置 API Key
- 前端: Chat 页面 token 选择器 + iframe
- Docker 网络: new-api 连接 librechat_librechat-net
- LibreChat Custom Endpoint "OpenToken" → https://api.opentoken.io/v1

### 待解决
- iframe 中 LibreChat 登录状态传递
- commit: `e3e6c5ef`
