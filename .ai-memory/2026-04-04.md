# 2026-04-04 工作日志

## 渠道代理功能检查
- 用户需求：每个 key 配独立代理，环境隔离
- 检查结果：代理功能代码完整，支持 HTTP/HTTPS/SOCKS5/SOCKS5H
- 覆盖：普通 API、AWS Bedrock、Vertex AI、Coze 请求
- 限制：代理是渠道级别，不是 key 级别
- 关键文件：
  - `dto/channel_settings.go` — `Proxy` 字段
  - `service/http_client.go:86` — `NewProxyHttpClient()` 实现
  - `relay/channel/aws/relay-aws.go:55` — Bedrock 代理支持
  - `relay/channel/api_request.go:489` — 通用 API 代理支持
- 建议方案：每个 key 单独建渠道，各配自己的代理（零开发）

## Prompt Caching 讨论
- 用户问 web 聊天是否能启用 prompt caching
- 当前 Bedrock 黑名单屏蔽了 `prompt-caching-2024-07-31`
- 所以所有经过 OpenToken 转发到 Bedrock 的请求都不会触发缓存
- 如需开启：把该 flag 从黑名单移到白名单
