# 2026-04-02 工作日志

## 蒸馏检测引擎 (commit `89a35a33`)
- 完成蒸馏检测功能，两个高准确率指标：
  1. 请求间隔标准差 < 500ms（机器人特征）
  2. 连续100次 max_tokens 完全一致（模板化采集）
- 两个同时触发才告警，累计3次禁用 Key
- 后端: `distillation/detector.go` + `distillation/controller.go`
- 前端: `SettingsDistillation.jsx`，位于运营设置底部
- Hook 在 `controller/relay.go` Relay() 函数，异步 goroutine
- 需要 Redis 支持，白名单机制

## 蒸馏检测 bug 修复 (commit `5cd92f1c`)
- 前端 `loadSettings` 把 `/api/option/` 返回的数组当对象读，导致刷新后设置丢失
- 修复：先将数组 `[{key, value}]` 转成 map 再读取

## Bedrock cache_control.scope 清理 (commit `f6238912`)
- 问题：`cache_control.scope` 在请求体中，Bedrock 不支持，报 400
- 修复：`relay/channel/aws/dto.go` — 转发前遍历 system/messages 删除 scope 字段
- 双重防护：Header 过滤 beta flags + Body 清理 scope

## macOS 缩放适配（已还原）
- 尝试用 `zoom: 0.82` + `font-size: 12px` 适配 Mac Retina
- 用户觉得效果不好，还原到原始状态 (commit `be8947dd`)
- 结论：Mac/Windows 渲染差异暂不通过代码强制适配

## API 渠道切换
- 从 `api.3mll.com` 切换到 `api.opentoken.io`（用户自己的 OpenToken 生产环境）
- Key 换过两次，最终用 `sk-xxGhjXBE...`

## 模型广场字重优化
- 卡片视图：价格、倍率、tag 加 font-medium，提升透明度
- 表格视图：倍率和价格数值加 font-medium，90% 透明度
- 已部署开发环境

## 全环境部署
- 蒸馏检测 + cache_control.scope 修复已部署到测试+生产 (`f6238912`)
- 模型广场优化目前只在开发环境

## 关于 prompt caching 的讨论
- 用户问多轮对话是否触发缓存计费
- 当前 Bedrock 黑名单屏蔽了 `prompt-caching-2024-07-31`，所以实际未触发
- AWS Bedrock 侧的缓存需要对应 beta flag 才生效

## 测试脚本
- 写了 `scripts/test_distillation.sh` 用于模拟蒸馏特征测试
