# 2026-03-16

## Aurora UI — Continued Polish (00:30–01:14 UTC)

### PricingCardView & Skeleton Rewrite
- Rewrote `PricingCardView.jsx`: removed Semi Card/Avatar/Tag deps, pure Tailwind div cards
  - Icon 48→40px, model name text-sm, price text-[11px], tags as plain span pills
  - gap-4→gap-3, tighter padding, no shadows
- Rewrote `PricingCardSkeleton.jsx`: pure CSS animate-pulse, no Semi Skeleton, default count 100→12

### RechargeCard (Topup Page) Redesign
- Removed gradient cover-4.webp blue banner entirely
- Stats: 3-col divide-x grid with 10px uppercase labels (matches UserInfoHeader style)
- Preset amount cards: replaced Semi Card with div border cards, selected state = border-foreground
- Outer wrapper: border card instead of Card+shadow, ghost 账单 button
- Removed Avatar dependency from header

### Home Page Hero Simplification
- Title: 7xl bold → 5xl semibold, removed shine-text effect
- Subtitle: muted-foreground color
- Buttons: rounded-full → rounded-lg
- Provider icons: 20 → 10, at 40% opacity
- Removed unused lobehub icon imports (Zhipu, Volcengine, Cohere, Suno, Minimax, Wenxin, Spark, Qingyan, Hunyuan, Xinference)
- max-w-4xl → max-w-2xl

### Bug Fixes
- **模型广场 scroll**: `.pricing-content` was missing `overflow-y: auto` — content below fold was unreachable
- **Price info layout**: `formatPriceInfo()` changed from inline spans to `flex-col gap-0.5` — prices now stack vertically (输入/补全/缓存读取/缓存创建 each on own line)

### Commits
- `10500b91` — pricing cards, topup page, home hero redesign (-700 +236 lines)
- `66a7af1d` — pricing scroll fix + price info vertical layout
- Both pushed to nomoapi + opentoken repos

### User Feedback Loop
- 抱抱熊 reported: 模型广场 can't scroll down, price items all horizontal and ugly
- Fixed both issues, deployed, awaiting visual confirmation

## OpenToken Rebrand + Interaction Fixes (01:14–04:30 UTC)

### Brand: Aurora → OpenToken
- Default system name → OpenToken, HTML title, Footer, Favicon
- New SVG logo: token ring + key design, monochrome (`/opentoken-logo.svg`)
- Sidebar menu renames: 概况预览 / 密钥管理 / 日志详情
- 7 locale translations added

### Language Selector Fix
- Rewrote `LanguageSelector.jsx` to use Radix DropdownMenu directly (was going through compat Dropdown which caused blank page navigation)

### SideSheet Placement
- All create/add modals changed from left→right slide (8 modal files)

### System Settings Fix (Critical)
- `OtherSetting.jsx`: submitSystemName/submitLogo now write to localStorage immediately
- `Form.jsx`: added `id={field}` to `<input>` and `<textarea>` — OtherSetting's handleInputChange reads `e.target.id` which was empty without this

### Alignment Fixes
- Header nav: font-semibold→text-sm font-medium, h-9 unified with action buttons
- Login/register: py-[10px]→h-9, text-xs→text-sm
- ThemeToggle icons: 18→16px
- Token page: removed size='small' from buttons (h-8→h-9 to match inputs)
- Ghost buttons: added `border border-border` (查询/重置 visibility)
- CardPro: header moved from Card title prop into body (divider alignment fix)
- TokensFilters: `items-center`→`items-end` (bottom alignment)
- FormField: `pure` prop now correctly passed from all 10 form components (was always undefined → mb-4)

### Tags
- v0.4.0-opentoken through v0.4.5-opentoken
- All pushed to both GitHub repos

### Checkpoint
- Project tracker `/opt/apps/newapis/newapi.md` fully updated
- 抱抱熊 says project is basically complete, wants checkpoint for future pickup
- **Resume keyword**: "继续 OpenToken 项目" or "看看 /opt/apps/newapis/newapi.md"

## Usage Logs Stat Bar Fix (08:10–08:14 UTC)

- Replaced 3 colored Tag components (blue/pink/white with `padding:13` + `box-shadow`) with unified stat bar
- New design: border card with `divide-x`, uppercase muted labels, `tabular-nums` values, no colons
- Added `mt-2` to fix overlap with card top border
- Skeleton: pure CSS pulse divs instead of Semi Skeleton
- Commit `873417b2` (redesign) + `efc596d9` (mt-2 fix)

## Production Deployment: 38.58.59.161 (08:10–08:14 UTC)

### Server Info
- **IP**: 38.58.59.161
- **Credentials**: root / ***REDACTED***
- **OS**: Ubuntu, 31GB RAM, 372GB disk
- **Domain**: api.opentoken.io
- **Code path**: `/opt/apps/opentoken` (cloned from `xizaihui/opentoken` feat/shadcn-ui)

### What was deployed
- Git clone latest `feat/shadcn-ui` branch
- Docker build `newapi-aurora:latest` on the server
- `docker compose up -d` — 4 containers: new-api, postgres, redis, elasticsearch
- Port 3000 bound to 127.0.0.1 only (no direct external access)

### Nginx (installed directly, not Docker)
- Config: `/etc/nginx/sites-available/opentoken`
- HTTP → HTTPS redirect
- Upstream keepalive 512 connections
- Streaming: `proxy_buffering off`, `X-Accel-Buffering no`, `chunked_transfer_encoding on`
- Timeout: read/write 18000s (5 hours), connect 60s
- WebSocket support via Upgrade/Connection headers
- Gzip enabled
- `worker_processes auto`, `worker_connections 65535`, `worker_rlimit_nofile 65535`

### SSL
- Let's Encrypt cert via certbot, expires 2026-06-14
- Auto-renewal: certbot systemd timer + cron backup at 3:00/15:00 daily
- Script: `/opt/apps/opentoken/renew-ssl.sh`

### System Tuning
- `somaxconn=65535`, `file-max=1000000`, `ip_local_port_range=1024-65535`
- `tcp_tw_reuse=1`, `tcp_fin_timeout=10`, `conntrack_max=1000000`
- `/etc/security/limits.d/opentoken.conf` — nofile 1000000

## Model Pricing Configuration (13:17–13:27 UTC)

### Task
Configure official pricing for all models using Method 1 (ModelRatio + CompletionRatio per-token billing).

### Database Backup
- Created: `/opt/apps/opentoken/backup-20260316-131735.sql.gz` (14K)

### What Was Updated
- **ModelRatio**: 133+ models updated to official input pricing
  - Base unit: ratio 1.0 = $2/M input tokens (GPT-3.5-turbo equivalent)
  - Key ratios: claude-opus-4-6=2.5, claude-sonnet-4-6=1.5, gpt-4.1=1, gpt-4o=1.25, gpt-5=0.625, o3=1, o3-pro=10, gemini-2.5-pro=0.625, gemini-2.5-flash=0.075
- **CompletionRatio**: output/input price ratio per model
  - Claude all=5, GPT-4.1/4o/o3/o4-mini=4, GPT-5=8, Gemini 2.5 Flash=23.33, Gemini 2.5 Pro=8
- **GroupRatio**: default/vip/svip all set to 1 (cost price, no markup)
  - 抱抱熊 mentioned "group ratio 7" but didn't confirm — left at 1, asked for clarification

### Pricing Sources Fetched
- OpenAI: openai.com/api/pricing + developers.openai.com/api/docs/pricing
- Anthropic: platform.claude.com/docs/en/about-claude/models/overview
- Google: cloud.google.com/vertex-ai/generative-ai/pricing

### Models Covered
- OpenAI: GPT-5.4, GPT-5/mini/nano, GPT-4.1/mini/nano, GPT-4o/mini, GPT-4 legacy, GPT-3.5, o1/o1-pro/o3/o3-pro/o4-mini, deep-research, embeddings, TTS, whisper
- Anthropic: Claude Opus 4.6, Sonnet 4.6, Opus 4.5/4.1/4.0, Sonnet 4.5/4.0/3.7/3.5/3.0, Haiku 4.5/3.5/3.0, AWS Bedrock format
- Google: Gemini 2.5 Pro/Flash/Flash-Lite, 2.0 Flash, 1.5 Pro/Flash, embeddings
- DeepSeek: chat/coder/reasoner, R1, V3

### Service Restarted
- `docker restart new-api` — confirmed API healthy via `curl https://api.opentoken.io/api/status`

### Open Question
- 抱抱熊 asked "claude-sonnet-4-6价格怎么没有" — but it IS in the data (ratio 1.5, completion 5). Asked which page they're looking at. Awaiting reply.
